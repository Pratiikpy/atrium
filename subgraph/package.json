{
  "name": "@atrium/subgraph",
  "version": "0.1.0",
  "description": "Scribe, Atrium event indexer",
  "license": "MIT",
  "private": true,
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio atrium",
    "create-local": "graph create --node http://localhost:8020/ atrium-local",
    "remove-local": "graph remove --node http://localhost:8020/ atrium-local",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 atrium-local",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.84.0",
    "@graphprotocol/graph-ts": "0.35.0"
  },
  "devDependencies": {
    "assemblyscript": "0.19.10",
    "matchstick-as": "0.6.0"
  }
}
