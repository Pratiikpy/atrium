{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "next build",
  "installCommand": "npm install --no-audit --no-fund --legacy-peer-deps",
  "crons": [
    { "path": "/api/cron/pyth-push", "schedule": "* * * * *" }
  ]
}
