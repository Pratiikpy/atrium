{
  "//": "Audit U-32: pre-fix this file didn't exist. The `build: tsc` script with no source-file args + no tsconfig prints the help screen and exits 0, every CI run silently passed. Same shape as codex/audit-U-31. Added: ES2022 + ESNext modules (matches `\"type\": \"module\"` in package.json), strict, @types/node already in devDependencies for the Buffer/crypto/process surfaces the attestor uses.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2022"],
    "types": ["node"],
    "strict": false,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "outDir": "dist",
    "rootDir": "src",
    "declaration": false,
    "sourceMap": true
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "dist", "src/**/*.test.ts"]
}
