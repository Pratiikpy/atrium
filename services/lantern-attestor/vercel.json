{
  "version": 2,
  "buildCommand": "echo 'no build'",
  "outputDirectory": "api",
  "installCommand": "pnpm install --no-frozen-lockfile",
  "functions": {
    "api/cron.ts": {
      "maxDuration": 60
    }
  }
}
